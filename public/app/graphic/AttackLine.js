import Tail from './Tail';

export default class AttackLine extends Tail {
    constructor(opts) {
        super(opts);

        this.displayName = 'AttackLine';
    }

    // render() {
    //     return super.render();
    // }
}